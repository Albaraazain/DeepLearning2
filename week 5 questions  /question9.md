Exam Question 3: Adaptive Optimization Methods
Compare global (fixed or uniformly decaying) learning rate methods with adaptive optimization methods such as AdaGrad, RMSProp, and Adam. In your answer, explain:

How adaptive methods adjust the learning rate on a per-parameter basis using accumulated gradient information or exponential moving averages.

The motivation behind these adaptive approaches, particularly in the context of non-uniform learning dynamics across parameters.

Any practical challenges or considerations (e.g., initial accumulation effects, the need for bias correction, or momentum components) when using these adaptive optimizers.