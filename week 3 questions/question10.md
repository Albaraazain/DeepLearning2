Exam Question 4: Cross Entropy Loss and Softmax Activation
Derive the cross entropy loss function for a multi-class classification problem using softmax activation. In your answer, be sure to:

Show how the softmax function converts raw scores (logits) into a probability distribution over classes.

Explain how one-hot encoding of the labels simplifies the cross entropy loss expression to a negative log likelihood for the correct class.

Discuss why frameworks combine the softmax operation with the logarithm for numerical stability.

