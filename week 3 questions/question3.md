Exam Question 3: Role of Regularization in Linear Models
Discuss the importance of regularization when training linear models. In your answer, address:

Why large weights can lead to overfitting and unstable decision boundaries.

How L2 regularization (weight decay) is integrated into the loss function to penalize large weights.

The trade-offs involved in balancing data loss (prediction error) and the regularization term during training.