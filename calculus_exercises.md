# Calculus Exercises

## Derivatives and Functions
1. Plot the function y = f(x) = x³ - x¹ and its tangent line when x = 1
2. Find the gradient of the function f(x) = 3x²₁ + 5eˣ²
3. Find the gradient of the function f(x) = ‖x‖²
4. Write out the chain rule for the case where:
   - u = f(x, y, z)
   - x = x(a, b)
   - y = y(a, b)

## Second Derivatives and Backpropagation
1. Explain why the second derivative is much more expensive to compute than the first derivative
2. Experiment with running the backpropagation function twice in succession and analyze the results
3. Analyze the control flow example calculating the derivative of d with respect to a

## Optimization Problems
1. Given data points x₁, ..., xₙ ∈ R, find a constant b that minimizes Σᵢ(xᵢ - b)²
