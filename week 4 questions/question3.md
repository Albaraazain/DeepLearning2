Exam Question 3: Converting Scores to Probabilities using Softmax
Explain the role of the softmax function in multi-class classification. In your answer, address:

How raw scores (logits) from a linear layer are converted into a probability distribution over classes.

Techniques used to ensure numerical stability when computing softmax (e.g., subtracting the maximum logit).

The effect of temperature scaling on the sharpness (peakedness) of the output distribution and its implications for training.

